object TestTags {
    object FileSaverDialog {
        const val dialog = "FileSaverDialog"
    }

    object Components {
        const val navRail = "NavRail"
    }

    object Buttons {
        const val regular = "RegularButton"
        const val outlined = "OutlinedButton"
        const val text = "TextButton"
        const val floating = "FloatingActionButton"
        const val extendedFloating = "ExtendedFloatingActionButton"
        const val icon = "IconButton"
        const val filledIcon = "FilledIconButton"
        const val iconToggle = "IconToggleButton"
    }

    object Cards {
        const val elevated = "ElevatedCard"
        const val outlined = "OutlinedCard"
        const val filled = "FilledCard"
    }

    object CheckBox {
        const val regular = "Regular"
    }

    object Chips {
        const val regular = "Regular"
    }

    object Divider {
        const val regular = "Regular"
    }

    object ProgressBar {
        const val circular = "CircularProgressBar"
    }

    object Slider {
        const val regular = "Regular"
    }

    object Switch {
        const val regular = "Regular"
    }

    object NavRailButtons {
        const val buttons = "Buttons"
        const val appBars = "AppBars"
        const val cards = "Cards"
        const val textFields = "TextFields"
        const val chips = "Chips"
        const val switch = "Switch"
        const val checkBox = "CheckBox"
        const val sliders = "Sliders"
        const val progressBar = "ProgressBar"
        const val dividers = "Dividers"
    }
}